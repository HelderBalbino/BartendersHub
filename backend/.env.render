# Render Environment Variables Template
# Copy these key-value pairs to your Render web service environment variables

# Required - Basic Configuration
NODE_ENV=production
PORT=10000

# Required - Database
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/bartendershub?retryWrites=true&w=majority

# Required - JWT Authentication
JWT_SECRET=CHANGE_ME_TO_A_STRONG_32+_CHAR_SECRET
JWT_EXPIRE=7d

# Required - Cloudinary (Image Storage)
CLOUDINARY_CLOUD_NAME=<your-cloudinary-cloud>
CLOUDINARY_API_KEY=<your-cloudinary-api-key>
CLOUDINARY_API_SECRET=<your-cloudinary-api-secret>

# Required - CORS
FRONTEND_URL=https://your-frontend-domain.com

# Optional - Email (for password reset, welcome emails)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=<your-email>
EMAIL_PASSWORD=<your-16-character-app-password>

# Optional - Rate Limiting (defaults are fine)
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=15

# Optional - File Upload (defaults are fine)
MAX_FILE_SIZE=5000000
UPLOAD_PATH=./uploads/

# Instructions:
# 1. Replace all "your-*" values with actual credentials
# 2. Generate a strong JWT_SECRET (32+ random characters)
# 3. Use your actual MongoDB Atlas connection string
# 4. Use your actual Cloudinary credentials
# 5. Update FRONTEND_URL with your deployed frontend domain
